{
  "name": "@package/infisical-runner",
  "version": "0.2.7",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "build:ci": "tsc -p tsconfig.json",
    "check": "biome check .",
    "lint": "eslint .",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --exclude \"dist/**\"",
    "test:ci": "vitest run --exclude \"dist/**\"",
    "test:watch": "vitest --exclude \"dist/**\"",
    "test:watch:no-infisical": "vitest --exclude \"dist/**\""
  },
  "devDependencies": {
    "@package/eslint": "workspace:*",
    "@package/tsconfig": "workspace:*"
  }
}
